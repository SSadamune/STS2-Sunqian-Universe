---
description: 卡牌/能力/遗物改名时须全仓库同步（本地化、代码、资源、伴生文件）
alwaysApply: true
---

# 卡牌内容改名全量同步

修改卡牌/能力/遗物的**显示名（任何语言的 title）、本地化键、StableEntryStem、类名**等命名时，**一次性**完成下列全部项，不要等用户补充。即使只修改了某一语言的显示名文本，也必须同步更新所有内部标识符（类名、StableEntryStem、L10n 键名、资源文件名等），使其与新名称保持一致。

## 必做清单

1. **本地化 JSON**（`sunqian-universe/localization/{eng,zhs}/` 下所有相关表：`cards.json`、`powers.json`、`relics.json` 等）
   - 重命名键名（如 `SUN_QIAN_SCRIPT` → `SUNQIAN_SCRIPT`）
   - 同步更新 **所有语言** 的 `title`、`description`、`smartDescription` 及附属键（`selectionScreenPrompt` 等）

2. **C# 注册与类型**
   - `[RegisterCard(..., StableEntryStem = "...")]` / 能力类名与文件名
   - 类名、构造函数、`PowerCmd.Apply<T>`、`typeof`、模式匹配、`SquMod` 等所有引用
   - 关联 Power 类一并改名

3. **资源路径**
   - `CardAssetProfile.PortraitPath`、`PowerAssetProfile.IconPath` / `BigIconPath`
   - 重命名 `images/cards/`、`images/powers/` 下 PNG，路径与文件名一致

4. **Godot 伴生文件**（详见 `godot-rename-sidecar-files.mdc`）
   - 删除旧名 `*.import`、`*.uid`
   - 为新 PNG 添加/更新 `*.import`（`source_file`、`dest_files` 指向新路径）
   - 为新 `*.cs` 添加 `*.cs.uid`（单行 `uid://...`）
   - Windows 仅改大小写时须两步 `git mv`（经临时文件名）

5. **收尾验证**
   - `rg` 旧名（类名、stem、键名、图片名）应为零结果
   - `dotnet build` 通过

## 卡牌名称引用

在 PR 描述、commit message、文档、对话回复等**任何**提及 Mod 内卡牌/能力/遗物中文名时：

1. **必须**从 `sunqian-universe/localization/zhs/` 对应表的 `.title` 读取正式译名（`cards.json`、`powers.json`、`relics.json` 等）
2. **禁止**根据英文类名、Stem 或原版直译自行翻译中文名

反例：`The Arrogant Fall` → ❌「傲慢者陨落」　✅「骄兵必败」（见 `cards.json`）

整理或展示卡牌列表时同样遵守上述规则。

## 命名约定速查

| 用途 | 示例 |
|------|------|
| `StableEntryStem` | `sunqian_script`（snake_case） |
| 卡牌 L10n 键 | `SUNQIAN_UNIVERSE_CARD_SUNQIAN_SCRIPT.title` |
| 能力 L10n 键 | `SUNQIAN_UNIVERSE_POWER_SCRIPT_SUNQIAN_POWER.title` |
| 卡牌图 | `images/cards/SunqianScript.png` |
| 能力图 | `images/powers/ScriptSunqianPower.png` |
