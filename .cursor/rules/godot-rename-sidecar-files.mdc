---
description: 重命名/移动 Godot 资源后清理并更新 .uid 与 .import 伴生文件
alwaysApply: true
---

# Godot 重命名伴生文件

重命名或移动项目内文件（尤其是 `Cards/`、`images/`、`scenes/` 等 Godot 资源）时，除改代码引用外还须处理伴生文件。若涉及卡牌/能力**显示名或 L10n 键**变更，先按 `card-content-rename-sync.mdc` 做全量同步，再执行下列伴生文件步骤：

1. **删除旧名伴生文件**：移除原路径下的 `*.uid`、`*.import`（以及仅绑定旧名的 `.gd.uid` 等），避免 Godot 残留无效条目。
2. **为新文件补齐伴生文件**：
   - C# 脚本：为新 `*.cs` 添加对应 `*.cs.uid`（单行 `uid://...`，与同目录其他脚本一致）。
   - 图片等资源：为新资源添加/更新 `*.import`，`source_file` 与 `dest_files` 中的路径须与新文件名一致。
3. **全局搜索**：用旧文件名 grep 仓库，确认无遗漏引用。

Godot 编辑器打开项目后可能再调整 import 哈希；重命名时先保证路径与文件名一致即可。
